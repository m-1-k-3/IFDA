# 需求文档:IoT 固件二进制自动化分析系统

**文档版本:** 1.0
**日期:** 2026-06-10
**状态:** 草稿

## 1. 引言

### 1.1 目的
本文档规定了一套面向 IoT 固件二进制的自动化分析系统的功能性与非功能性需求。该系统直接对固件镜像及其所含可执行文件执行漏洞挖掘与二进制逆向工程,其明确目标是无需分析人员手动解包即可运行。

### 1.2 范围
系统接收固件镜像(或已提取的二进制),自动识别并解包其内容,并产出涵盖二进制结构、攻击面与候选漏洞的结构化分析结果。它面向安全研究人员、产品安全团队,以及对自有设备或经授权的第三方设备进行防御性评估的厂商使用。

### 1.3 预期用途与授权
本工具用于防御与研究用途。操作者有责任确保其对所提交分析的任何固件拥有授权。系统应支持审计追踪,记录何人在何时分析了何物。

### 1.4 术语定义
- **固件镜像(Firmware image)** —— 厂商分发的二进制数据块,通常包含引导加载程序、内核、一个或多个文件系统以及配置数据。
- **"无需解包(Without unpacking)"** —— 分析人员无需手动提取镜像;系统自动完成识别、雕取(carving)与提取。(在内部,系统仍会提取内容以进行分析。)
- **攻击面(Attack surface)** —— 网络监听端口、IPC 端点、暴露的服务、处理不可信输入的解析器。

## 2. 系统概述

系统采用流水线结构:

1. **接收(Ingestion)** —— 接受固件镜像或二进制,进行归一化,记录元数据。
2. **识别与提取(Identification & extraction)** —— 识别容器/文件系统格式并自动提取内容。
3. **清点与分类(Inventory & classification)** —— 枚举可执行文件、库、脚本、配置文件以及证书/密钥。
4. **静态分析(Static analysis)** —— 反汇编、反编译、控制流/数据流重建。
5. **漏洞挖掘(Vulnerability discovery)** —— 模式匹配、已知 CVE 关联、污点分析,以及可选的动态/仿真执行。
6. **报告(Reporting)** —— 带有证据与复现上下文的结构化、按优先级排序的发现。

## 3. 功能性需求

### 3.1 接收(FR-ING)
- **FR-ING-1** 接受常见的固件交付格式:原始 `.bin`、厂商更新包、`.img`,以及归档容器(zip、tar、cpio 等)。
- **FR-ING-2** 计算并存储输入及每个提取产物的加密哈希(SHA-256),用于溯源与去重。
- **FR-ING-3** 在提取之前检测并记录输入的高层类型(例如完整镜像与单个 ELF 的区别)。
- **FR-ING-4** 支持批量提交与排队,以适应大规模语料分析。

### 3.2 自动识别与提取(FR-EXT)
- **FR-EXT-1** 自动识别内嵌文件系统(SquashFS、JFFS2、UBIFS、CramFS、ext2/3/4、romfs)以及容器/压缩格式(gzip、LZMA、XZ、LZ4、zlib)。
- **FR-EXT-2** 无需操作者介入即可递归雕取并提取嵌套内容,并提供可配置的递归深度上限以防止提取失控。
- **FR-EXT-3** 检测引导加载程序头部及常见厂商封装(例如 U-Boot、TRX 及类似的头部格式)并解析其元数据。
- **FR-EXT-4** 检测加密或混淆区域并加以标记,而非静默失败;报告熵分析以区分压缩与加密。
- **FR-EXT-5** 保留提取树(偏移量、父/子关系),使任何产物都能追溯回其在原始镜像中的位置。

### 3.3 清点与分类(FR-INV)
- **FR-INV-1** 枚举所有可执行文件与共享库,记录架构(ARM、MIPS、x86、RISC-V 等)、字节序、位宽与 ABI。
- **FR-INV-2** 在可推导的情况下,识别构建工具链、libc 变体(uClibc、musl、glibc)与编译器版本。
- **FR-INV-3** 检测解释型脚本(shell、Lua、Python)、init/启动脚本与服务定义。
- **FR-INV-4** 定位内嵌的机密信息与凭据材料:私钥、证书、硬编码的口令/哈希、API 令牌。并附带置信度级别进行报告。
- **FR-INV-5** 提取版本字符串与第三方组件的软件物料清单(SBOM)。

### 3.4 二进制逆向工程(FR-RE)
- **FR-RE-1** 对所有受支持的架构反汇编可执行文件。
- **FR-RE-2** 产出供分析人员审阅的反编译伪代码。
- **FR-RE-3** 重建控制流图与调用图;识别函数边界。
- **FR-RE-4** 识别标准库函数与常见的静态链接例程(基于签名,例如 FLIRT 风格)以降低分析人员的噪声。
- **FR-RE-5** 检测每个二进制启用的漏洞缓解措施:NX、栈金丝雀(stack canaries)、RELRO、PIE/ASLR 兼容性、强化源码(fortified source)。
- **FR-RE-6** 为字符串、导入与函数调用提供交叉引用,以支持手动跳转分析。
- **FR-RE-7** 通过交互式界面与可脚本化 API 两种方式暴露结果,以支持自动化。

### 3.5 漏洞挖掘(FR-VUL)
- **FR-VUL-1** **已知漏洞关联:** 将识别出的组件与版本同漏洞数据库(CVE/NVD 及同类)进行匹配,以标记已知存在漏洞的软件。
- **FR-VUL-2** **危险函数检测:** 定位不安全 API 的使用(`strcpy`、`sprintf`、`system`、`popen`、长度受攻击者影响的 `memcpy`、命令执行汇聚点)。
- **FR-VUL-3** **污点/数据流分析:** 追踪数据从不可信来源(网络输入、`nvram`/配置读取、环境变量、请求参数)流向敏感汇聚点(内存拷贝、命令执行、格式化字符串),并报告可达路径。
- **FR-VUL-4** **漏洞类别覆盖:** 缓冲区溢出、命令注入、格式化字符串缺陷、流入分配/拷贝的整数溢出、路径遍历、弱/硬编码加密的使用,以及认证逻辑弱点。
- **FR-VUL-5** **跨二进制分析:** 跨越二进制/库边界跟踪调用与数据(例如某个 CGI 处理程序调用进共享库)。
- **FR-VUL-6** **可选的仿真/动态分析:** 在可行的情况下,仿真固件或单个服务以验证可达性并减少误报。该功能应为可选且置于沙箱中。
- **FR-VUL-7** **发现优先级:** 每条发现都附带严重性、置信度、受影响组件、证据(反汇编/反编译片段、污点路径)与修复建议。
- **FR-VUL-8** **抑制与分诊状态:** 允许分析人员将发现标记为已确认、误报或可接受风险,并在重新扫描后保持持久化。

### 3.6 报告与输出(FR-REP)
- **FR-REP-1** 生成机器可读的输出(JSON)与人类可读的报告(HTML/PDF/Markdown)。
- **FR-REP-2** 以标准格式(例如 CycloneDX 或 SPDX)产出 SBOM。
- **FR-REP-3** 支持在固件版本之间进行差异比对,以突出新引入或已修复的问题。
- **FR-REP-4** 提供执行摘要,以及带有复现证据的逐条发现详细章节。

### 3.7 集成与可扩展性(FR-INT)
- **FR-INT-1** 暴露 REST/CLI API 用于提交与结果检索,以支持 CI/CD 集成。
- **FR-INT-2** 支持插件架构,使新的提取器、分析器与检测规则无需修改核心即可添加。
- **FR-INT-3** 允许用户自定义检测规则(例如 YARA 风格的签名、自定义的污点源/汇聚点定义)。

## 4. 非功能性需求

### 4.1 性能与可扩展性(NFR-PERF)
- **NFR-PERF-1** 在可配置的时间预算内端到端分析一个典型的消费级路由器镜像(≤64 MB)(目标:在参考硬件上静态分析耗时低于 30 分钟)。
- **NFR-PERF-2** 通过并行工作节点横向扩展,以处理数千个镜像的语料库。
- **NFR-PERF-3** 在镜像之间缓存并去重相同的产物,以避免重复分析。

### 4.2 准确性(NFR-ACC)
- **NFR-ACC-1** 报告置信度分数;对高严重性发现优先保证精确率(precision),同时将较低置信度的线索单独呈现。
- **NFR-ACC-2** 针对带标注的基准语料库跟踪误报/漏报指标。

### 4.3 安全与隔离(NFR-SEC)
- **NFR-SEC-1** 将所有固件视为不可信;在沙箱/隔离环境中执行提取与任何仿真,以防止恶意镜像危害宿主机。
- **NFR-SEC-2** 强制实施资源限制(CPU、内存、磁盘、递归深度),以缓解解压炸弹与畸形输入。
- **NFR-SEC-3** 维护访问控制以及对提交与分析的审计日志。
- **NFR-SEC-4** 保护提取出的机密信息与发现在静态存储与传输过程中的安全。

### 4.4 架构覆盖(NFR-ARCH)
- **NFR-ARCH-1** 至少支持 ARM(32/64)、MIPS(LE/BE)、x86/x86-64。其他架构(RISC-V、PowerPC、SuperH)为可选项。

### 4.5 易用性与可维护性(NFR-USE)
- **NFR-USE-1** 提供清晰的日志,并在个别阶段失败时呈现部分结果(优雅降级)。
- **NFR-USE-2** 使漏洞数据库与签名集可独立于版本发布进行更新。
- **NFR-USE-3** 为 API、插件接口与规则格式提供文档。

### 4.6 部署(NFR-DEP)
- **NFR-DEP-1** 支持本地部署与容器化部署;避免强制将客户固件传输至第三方服务。

## 5. 约束与假设
- 加密的固件镜像在无密钥的情况下可能部分或完全无法分析;系统对加密进行标记,而非破解。
- 反编译与污点分析属于启发式方法;结果需要分析人员验证。
- 假定操作者拥有分析所提交固件的合法授权。

## 6. 验收标准
- 给定一组具有代表性的未加密厂商固件镜像,系统能够提取内容、清点二进制并正确检测架构,并在无需手动解包的情况下产出按优先级排序的发现。
- 基准语料库中已知存在漏洞的组件能够通过 CVE 关联被正确标记。
- 一个植入的漏洞(例如来自网络输入的命令注入路径)能够被检测并报告,且附带可追溯的源到汇聚点路径。

## 7. 待解问题
- 规模化场景下所需的吞吐量/语料规模为多少?
- 哪些环节基于现有工具构建,哪些需自行实现(提取、反汇编/反编译引擎)?
- 首个版本中动态/仿真支持的深度如何?
- 输出需符合的合规/报告标准是什么?
